module.exports = {
    // Bot version
    version: "1.0.0",

    // Your WhatsApp number (receives the "Bot is online" ping)
    ownerNumber: "254797715445@s.whatsapp.net",

    // Command prefixes the bot will respond to
    prefixes: ["!", ".", "/"],

    // Baileys auth folder
    authFolder: "auth_info_baileys",

    // Max IDs to track for duplicate prevention
    processedIdLimit: 500,

    // Admins (in addition to owner) — add numbers like "2547...@s.whatsapp.net"
    admins: [],
};
